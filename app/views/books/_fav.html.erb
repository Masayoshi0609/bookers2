 <% if book.favorited_by?(current_user) %>
 		<%= link_to book_favorites_path(book), method: :delete do, remote:true %>
 		<label class="text-danger">♥<%= book.favorites.count %></label>
 		<% end %>
 <% else %>
 		<%= link_to book_favorites_path(book), method: :post do, remote:true %>
 		<label>♡<%= book.favorites.count %></label>
 		<% end %>
 <% end %>